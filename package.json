{
  "name": "tedi-remote-access",
  "version": "0.10.1",
  "private": true,
  "type": "module",
  "description": "Build tooling for the TEDI Remote Access extension. The shipped artifact is the bundled extension.js (built in CI into the release .zip, not committed); source lives in src/. The browser client and relay live in client/ and server/.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "check": "node --check extension.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
